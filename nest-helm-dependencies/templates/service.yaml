apiVersion: v1
kind: Service
metadata:
  name: {{ include "nest-helm-dependencies.fullname" . }}
  labels:
    {{- include "nest-helm-dependencies.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nest-helm-dependencies.selectorLabels" . | nindent 4 }}
