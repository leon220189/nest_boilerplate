apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: backend-skaffold-helm-backend-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: localhost
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: backend-skaffold-helm-backend-service
            port:
              number: 3000
