apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "postgres.name" . }}
  labels:
    app: {{ template "postgres.name" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  selector:
    matchLabels:
      app: {{ template "postgres.name" . }}
      release: {{ .Release.Name }}
  replicas: {{ .Values.global.postgres.replicaCount }}
  template:
    metadata:
      labels:
        app: {{ template "postgres.name" . }}
        release: {{ .Release.Name }}
    spec:
      volumes:
        - name: static-assets
          configMap:
            name: {{ template "postgres.name" . }}
            defaultMode: {{ .Values.global.postgres.defaultMode }}
      containers:
        - name: {{ .Chart.Name }}
          image: {{ .Values.global.postgres.image }}
          env:
          {{- range $key, $value := .Values.global.postgres.env }}
          - name: {{ $key }}
            value: {{ $value | quote }}
          {{- end }}
          imagePullPolicy: {{ .Values.global.postgres.pullPolicy }}
          ports:
            - containerPort: {{ .Values.global.postgres.containerPort }}
          volumeMounts:
            - mountPath: /usr/share/nginx/html/
              name: static-assets
          resources:
{{ toYaml .Values.resources | indent 12 }}
    {{- if .Values.nodeSelector }}
      nodeSelector:
{{ toYaml .Values.nodeSelector | indent 8 }}
    {{- end }}