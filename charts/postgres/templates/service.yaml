# templates/postgres-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}-postgres-service
  labels:
    app: {{ .Chart.Name }}-postgres
spec:
  type: ClusterIP
  selector:
    app: {{ .Chart.Name }}-postgres
  ports:
    - port: 5432
      targetPort: 5432
