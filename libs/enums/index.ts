export enum UserStatus {
	ACTIVE = 'Active',
	SUSPENDED = 'Suspended',
	LOCKED = 'Locked',
}

export enum Gender {
	MALE = 'Male',
	FEMALE = 'Female',
	OTHER = 'Other',
}

export enum Role {
	ADMIN = 'Admin',
	MANAGER = 'Manager',
	SUPERVISOR = 'Supervisor',
	EMPLOYEE = 'Employee',
}
